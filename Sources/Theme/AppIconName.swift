import ChunUI

enum AppIconName {
    static let overview = "grid-dashboard-bento"
    static let library = PikaIcon.Name.folder
    static let components = "diamond-component"
    static let settings = "settings01"
    static let search = PikaIcon.Name.search
    static let document = PikaIcon.Name.fileText
    static let tokens = "color-palette"
    static let checklist = PikaIcon.Name.listChecklist
    static let note = PikaIcon.Name.note
    static let close = PikaIcon.Name.close
}
